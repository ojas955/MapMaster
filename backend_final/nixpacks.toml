[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmdsPrefix = ["pip install"]
cmds = [
    "-r requirements.txt",
    "requests pdfplumber python-dotenv"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
